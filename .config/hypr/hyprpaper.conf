# hyprpaper >= 0.8 uses anonymous `wallpaper { }` blocks.
# The old `preload = ...` / `wallpaper = ,path` syntax is no longer parsed.
# See https://wiki.hypr.land/Hypr-Ecosystem/hyprpaper/

wallpaper {
    monitor =
    path = ~/Pictures/Wallpapers/clair_obscur.jpg
    fit_mode = cover
}
